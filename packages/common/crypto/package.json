{
  "name": "common-crypto",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "lint": "yarn run -T eslint .",
    "lint:fix": "yarn run -T eslint . --fix",
    "build": "echo 'Pure JS package - no build step'",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "common-logger": "workspace:*",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "vitest": "^4.0.18"
  }
}
