{
  "name": "@monorepo/ws-service",
  "version": "1.0.0",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "lint": "yarn run -T eslint src",
    "lint:fix": "yarn run -T eslint src --fix",
    "test": "yarn run -T vitest run"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
